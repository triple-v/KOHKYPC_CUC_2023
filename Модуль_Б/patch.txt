en	// Превилегированный режим

conf t	// Конфигурация

urlfilter list whitelist	// Создаём БД с разрешёнными сайтами

action permit	// Разрешаем запуск БД

redirect-server-ipv4 айпи веба	// Перенаправляет с заблокированного сайта на сайт WEB'а

url разрёшённый сайт	// Пишем в БД разрешённый сайт (можно повторять если сайтов несколько)

end	// Выход

reload	// Перезагрузка

Проверить функцию можно если в параметрах ВМ сменить сетевой адаптер в VNet8 Nat на обычный NAT (он не в выпадающем списке).
В браузере должны грузиться только заданные сайты, а остальные будут грузиться бесконечно.